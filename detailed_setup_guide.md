# ショッピングリマインダーアプリ：実機テスト完全ガイド

このドキュメントでは、作成したコードを実機（iPhone）で動かすための手順を、一から十まで具体的に解説します。

---

## 1. Supabase の準備 (バックエンド)

最初に、データを保存・共有するためのサーバー（Supabase）をセットアップします。

1. **プロジェクト作成:**
   - [Supabase公式サイト](https://supabase.com/) にログインし、「New Project」ボタンをクリックします。
   - プロジェクト名（例: `ShoppingReminder`）とデータベースパスワードを設定し、「Create new project」をクリックします。
2. **データベーススキーマの実行:**
   - 左メニューの **「SQL Editor」**（`>_` アイコン）をクリックします。
   - 「New Query」をクリックし、[schema.sql](file:///Users/kazuma/Projects/Shopping-Reminder/supabase/schema.sql) の中身をすべてコピー＆ペーストして **「Run」** をクリックします。
   - これで、リストやアイテムのテーブルが作成されます。
3. **APIキーの取得:**
   - 左メニューの **「Settings」**（歯車アイコン） > **「API」** を開きます。
   - `Project URL` と `anon (public)` キーをメモしておきます。

---

## 2. Xcode プロジェクトの作成とコードの統合

1. **新規プロジェクト:**
   - Xcodeを開き、**「File > New > Project」** を選択。
   - **App** を選択し、プロジェクト名を `ShoppingReminder` にします。
   - **Interface:** `SwiftUI`, **Language:** `Swift` であることを確認。
2. **ライブラリの追加:**
   - **「File > Add Packages...」** を開きます。
   - 右上の検索窓に `https://github.com/supabase-community/supabase-swift` を入力。
   - 依存関係を追加します。
3. **ファイルのコピー:**
   - 私が作成したファイルを、Xcodeプロジェクト内の対応する場所にコピー（または上書き保存）します。
   - 特に `SupabaseService.swift` を開き、先ほどメモした `URL` と `Key` を書き換えます。

---

## 3. 実機（iPhone）での実行設定

実機で動かすためには、「署名（Signing）」という作業が必要です。

1. **iPhoneを接続:**
   - MacとiPhoneをケーブルで繋ぎます。
2. **ターゲットの選択:**
   - Xcode上部の実行ボタンの右側をクリックし、自分のiPhoneを選択します。
3. **署名設定:**
   - 左側のファイル一覧の一番上（青いアイコンのプロジェクト名）をクリック。
   - 右側の **「Signing & Capabilities」** タブを開きます。
   - **Team:** 自分の名前（Apple ID）を選択します。
   - **Bundle Identifier:** 独自のIDに変更します（例: `com.kazuma.Shopping-Reminder` など。ドットで区切られた英数字）。
4. **実行:**
   - 左上の **「▶（実行ボタン）」** を押します。

---

## 4. iPhone側での「信頼」設定

初めて実行すると、iPhoneの画面に「信頼されていないデベロッパ」と表示されます。

1. iPhoneの **「設定」** を開く。
2. **「一般」 > 「VPNとデバイス管理」**（または「プロファイルとデバイス管理」）をタップ。
3. あなたのApple ID（デベロッパ）をタップし、**「"あなたのID"を信頼」** をタップ。

これでアプリが起動します！

---

## 5. テストの始め方

1. アプリを起動したら、まずは「＋」ボタンで新しいリストを作ってみてください。
2. 別のブラウザや別の端末（可能であれば）からSupabaseのテーブルにデータが入っているか確認できます。
3. 購入予定ボタンを押した時、Supabaseの `items` テーブルの `planning_purchaser_id` が更新されれば成功です。

> [!CAUTION]
> **プッシュ通知についての注意**
> 「アプリを閉じていても届くプッシュ通知」は、Appleの有料開発者プログラムへの登録が必要です。今回のコードには通知のロジック（Edge Function）を含めていますが、無料版ではアプリ内での状態更新（リアルタイム同期）の確認をメインで行ってください。
